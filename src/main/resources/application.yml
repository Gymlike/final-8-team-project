cloud:
  aws:
    credentials:
      secret-key: QQvtr4AAZvl3ZEtiGhAvhXT1kSOk9W6oXRetYRYH
      access-key: AKIA5CHINSZMB4NQUEDS
    s3:
      bucket: signer-image-url

    region:
      static: ap-northeast-2

    stack:
      auto: 'false'

spring:
  jpa:
    generate-ddl: 'true'
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: 'true'
        show_sql: 'true'
#        default_batch_fetch_size: 100
#        jdbc:
#          batch_size: 30

  # Mysql 설정(RDS )
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://team-database1.cdhsnkagrp4k.ap-northeast-2.rds.amazonaws.com:3306/mydb?serverTimezone=UTC&characterEncoding=UTF-8
    username: admin
    password: 8teamadmin

# h2
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:db;MODE=MYSQL;
#    username: sa
#    password:
#   Mysql 설정
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/mydb?serverTimezone=UTC&characterEncoding=UTF-8
#    username: woni
#    password: govlmysql

  mvc:
    hiddenmethod:
      filter:
        enabled: 'true '
  thymeleaf:
    cache: 'false'
  main:
    allow-bean-definition-overriding: 'true'

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 6



# Actuator
management:
  endpoint:
    metrics:
      enabled : true
    prometheus:
      enabled: true

  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health, metrics, prometheus, info
        # exclude: env은 Actuator 엔드포인트 중에서 "env"를 제외하겠다는 의미입니다.
        # "env" 엔드포인트는 애플리케이션의 환경 변수를 보여주는 엔드포인트이며,
        # 보안상의 이유로 기본적으로 제외되어 있습니다.
        # 굳이 해둔이유는 누군가
        # include를 "*"로 바꾸었을때를 위한 대비입니다.
        exclude: env

  metrics:
    tags:
      application: Final8TeamProjectApplication


logging:
  level:
    com.example.myapp: DEBUG

#이메일보내는것 설정

mail:
  smtp:
    port: '465'
    starttls:
      enable: 'true'
      required: 'true'
    socketFactory:
      fallback: 'false'
      class: javax.net.ssl.SSLSocketFactory
      port: '465'
    auth: 'true'

AdminMail:
  id: spartafinalproject@gmail.com
  password: wpzfionudzmbhaau

jwt:
  secret:
    key: 7ZWt7ZW0OTntmZTsnbTtjIXtlZzqta3snYTrhIjrqLjshLjqs4TroZzrgpjslYTqsIDsnpDtm4zrpa3tlZzqsJzrsJzsnpDrpbzrp4zrk6TslrTqsIDsnpA=


springdoc:
  override-with-generic-response: false
